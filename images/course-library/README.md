# Course Library images

These 120 local assets were copied from the supplied `All Courses` Teachable export.

Each filename uses a stable `course-library-###-descriptive-name` pattern so that duplicate and opaque Teachable filenames cannot overwrite one another. The original export on the Desktop has not been changed.

Use these local files for the native Course Library instead of hotlinking Teachable image URLs.
